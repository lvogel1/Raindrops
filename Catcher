class Catcher {
  PVector loc;
  int d;

  Catcher () {
    loc = new PVector (mouseX, height-100);
    d = 100;
  }

  void show () {
    fill (55, 0, 55);
    loc.x = mouseX;
    ellipse (loc.x, loc.y, d, d);
  }
}
