class Catcher {
  PVector loc;
  int d;

  Catcher () {
    loc = new PVector (mouseX, height-100);
    d = 100;
  }

  void show () {
    fill (120, 0, 130);
    loc.x = mouseX;
    ellipse (loc.x, loc.y, d, d);
  }
}
